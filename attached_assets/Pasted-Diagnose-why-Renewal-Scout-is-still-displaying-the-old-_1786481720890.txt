Diagnose why Renewal Scout is still displaying the old OpenAI error.

IMPORTANT:
- Do not change any source code.
- Do not make an authenticated OpenAI API request.
- Do not run Renewal Scout research.
- Do not display OPENAI_API_KEY or any other secret.
- Preserve all untracked attached_assets files.

1. Run git status and report the current branch and commit.

2. Fetch GitHub:
   git fetch origin

3. Confirm whether merge commit d00f001f5e13940aaefde7c43e93796c06cfb923 from PR #4 is present in the current checkout.

4. If local main is behind origin/main and there are no conflicting tracked changes:
   git switch main
   git pull --ff-only origin main

5. Confirm that artifacts/api-server/src/lib/research-service.ts contains all three of these:
   - timeout: 180_000
   - maxRetries: 0
   - AI research timed out before completion. No automatic retry was made.

6. Check the compiled server artifact for the same new timeout message and configuration. If the compiled artifact is stale, run:
   pnpm install --frozen-lockfile
   pnpm run typecheck
   pnpm run build

7. Completely stop the currently running Renewal Scout server and restart it from the newly built main branch. Confirm that the restart occurred after the build completed.

8. Read the latest four research run records from the database using only:
   - run ID
   - status
   - created time
   - started time
   - completed time
   - stored safe error message

   Do not display prompts, household information or secrets.

9. Identify whether the error the user is seeing belongs to an older run. Calculate how long the newest failed run lasted:
   - approximately 45 seconds suggests the old server code
   - approximately 180 seconds suggests the new timeout code

10. Perform this unauthenticated, non-billable connectivity check only:
    curl -sS -o /dev/null -w "HTTP %{http_code}, connect %{time_connect}s, first-byte %{time_starttransfer}s\n" --connect-timeout 10 https://api.openai.com/v1/models

    Do not add an Authorization header. A quick HTTP 401 is expected and proves Replit can reach OpenAI.

11. Report:
    - local commit SHA
    - origin/main SHA
    - whether PR #4 is installed
    - whether the compiled build is current
    - newest run ID, timestamp, duration and safe error
    - connectivity-check result
    - the most likely cause

Do not open a PR and do not perform another research request.